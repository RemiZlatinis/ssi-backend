# ==============================================================================
# SSI BACKEND - PRODUCTION ENVIRONMENT VARIABLES (TEMPLATES)
# ==============================================================================
# This file lists the environment variables required for Production or Staging.
# Use this as a reference for setting up CI/CD or your production host.
# ==============================================================================

# --- GENERAL SETTINGS ---
ENVIRONMENT=production
# Generate a strong unique key for production (e.g., 'openssl rand -base64 32')
SECRET_KEY=
# Ensure DEBUG is False in production
DEBUG=False

# --- NETWORK ---
# Comma-separated list of allowed hostnames (e.g., api.yoursite.com)
ALLOWED_HOSTS=api.service-status-indicator.com
# Comma-separated list of domain names (no protocol) for CSRF protection
HOST=api.service-status-indicator.com
# Comma-separated list of origins for CORS (e.g., https://app.yoursite.com)
CORS_ALLOWED_ORIGINS=https://service-status-indicator.com

# --- DATABASE & CACHE ---
# Primary PostgreSQL connection string (Mandatory)
DATABASE_URL=
# Connection string for Redis/Valkey (Mandatory for Channels)
REDIS_URL=

# --- S3 BACKUPS (OPTIONAL) ---
# All must be provided to enable automated S3 database backups
AWS_S3_ACCESS_KEY_ID=
AWS_S3_SECRET_ACCESS_KEY=
AWS_S3_REGION_NAME=
AWS_S3_ENDPOINT_URL=
AWS_STORAGE_BUCKET_NAME=

# --- MONITORING & LOGGING ---
# Sentry DSN for error tracking (Mandatory for production)
SENTRY_DSN=
# Global log level (Recommended: INFO or WARNING)
LOG_LEVEL=INFO
# Per-app log level overrides (Optional)
# LOG_LEVEL_DJANGO=INFO
# LOG_LEVEL_DJANGO_REQUEST=ERROR
# LOG_LEVEL_CORE=INFO
# LOG_LEVEL_NOTIFICATIONS=INFO
# LOG_LEVEL_AUTHENTICATION=INFO
# LOG_LEVEL_DBBACKUP_ADMIN=INFO

# --- INITIAL SETUP ONLY ---
# Credentials for the first administrator (used during bootstrap)
DJANGO_SUPERUSER_USERNAME=
DJANGO_SUPERUSER_EMAIL=
DJANGO_SUPERUSER_PASSWORD=
